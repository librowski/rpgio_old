import styled from 'styled-components';

export const Container = styled.div`
    position: relative;
    display: flex;
    width: fit-content;
    flex-wrap: wrap;
    overflow-x: hidden;
    overflow-y: auto;
`;
